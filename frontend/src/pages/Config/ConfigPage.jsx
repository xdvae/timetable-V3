import { PagePlaceholder } from "@/components/layout/placeholder.jsx";

export function ConfigPage() {
  return <PagePlaceholder title="Configuration" subtitle="Working days, periods, breaks, and size limits." route="/config" />;
}
